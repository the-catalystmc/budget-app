<%= link_to group_path(group) do %>
  <div class="cat">
<%= image_tag group.icon,
alt: 'icon' %>
<div class="cat-info">
      <h3><%= group.name %></h3>
      <h3  class="cat-expense"> $<%=group.total_expenses %></h3>
    </div>
  </div>
<% end %>